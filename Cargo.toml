[package]
name = "camilladsp-config"
version = "0.1.0"
edition = "2021"
description = "Configuration types for CamillaDSP - build and serialize YAML configs programmatically"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
